#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define ischar(x)  (('a' <= x && x <= 'z') || ('A' <= x && x <= 'Z'))
int main()
{
    int n;

    map<string ,int>m;

    scanf("%d",&n);

    string str[100];

    for(int i=0; i<3*n; i++)
    {
        cin>>str[i];
    }



}

